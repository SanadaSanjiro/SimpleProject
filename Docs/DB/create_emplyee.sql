create table employees
(
	id 				bigserial 	primary key,
	uid				varchar,
	firstname		varchar,
	lastname		varchar,
	surname			varchar,
	position		varchar,
	account			varchar,
	email			varchar,
	status			varchar
);